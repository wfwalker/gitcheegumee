<h1>Listing passages</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Source</th>
    <th>Destination</th>
  </tr>

<% @passages.each do |passage| %>
  <tr>
    <td><%=h passage.title %></td>
    <td><%=h passage.source_id %></td>
    <td><%=h passage.destination_id %></td>
    <td><%= link_to 'Show', passage %></td>
    <td><%= link_to 'Edit', edit_passage_path(passage) %></td>
    <td><%= link_to 'Destroy', passage, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New passage', new_passage_path %>